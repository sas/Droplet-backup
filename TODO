* Handle the -i option in restore
* Handle the -v option in backup and restore
* Do the purge command
* Do the stats command
* Write man pages
* When a command fails, it should display its own help instead of the general
  help
